{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "recruiting-agent.md"
  ],
  "mcp": {
    "project": {
      "type": "local",
      "command": [
        "uv",
        "run",
        "--directory",
        "mcp-server",
        "opencode-mcp"
      ],
      "enabled": true,
      "timeout": 15000
    }
  }
}
